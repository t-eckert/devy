# Devy Frontend

This directory contains the web view of Devy. It is a NextJS application responsible for 
communicating with the backend API and rendering content.

## Running the Frontend

``` shell
npm install
npm run dev
```
